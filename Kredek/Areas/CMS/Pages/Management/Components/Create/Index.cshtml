@page
@using Kredek.Data.Models.CreatableModels
@model Kredek.Areas.CMS.Pages.Management.Components.Create.IndexModel

<div class="row">
    <div class="col-md-4">
        <form method="post" enctype="multipart/form-data">
            <input type="hidden" asp-for="@Model.WebsitePageId" />

            <div asp-validation-summary="ModelOnly" class="text-danger"></div>

            <div class="form-group">
                <label asp-for="@Model.ContentElement.Position" class="control-label"></label>
                <input asp-for="@Model.ContentElement.Position" class="form-control" />
                <span asp-validation-for="ContentElement.Position" class="text-danger"></span>
            </div>

            <hr />

            @switch (Model.NewElement)
            {
                @*
                    looked good but didn't work :/ 

                    case ExampleCreatableModel model:
                    @await Html.PartialAsync("./Create", model.ExamplePropertyOneLine)
                    @await Html.PartialAsync("./Create", model.ExamplePropertyTextArea)
                    break;
                *@
                case ExampleCreatableModel model:
                    @await Html.PartialAsync("./CreateExample", Model.ExampleNewElement)
                    break;
            }

            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}

@*
    https://localhost:44328/CMS/Management/Components/Create/Index
*@